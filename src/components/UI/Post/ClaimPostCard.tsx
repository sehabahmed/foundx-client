"use client";

import { IReceiveClaimRequests } from "@/src/types";


type TProps = {
  post: IReceiveClaimRequests;
};

export default function ClaimPostCard({ post }: TProps) {
  
  // const { claimRequest, title, dateFound, de }
console.log("post", post);

  return (
    <>
    
    </>
  );
}
